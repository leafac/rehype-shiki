{
  "scripts": {
    "develop": "ts-node src/index.ts",
    "test": "prettier --check 'src/**/*' 'tests/**/*' && jest",
    "build": "tsc",
    "clean": "shx rm -rf lib"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "shx": "^0.3.3",
    "ts-jest": "^26.4.4",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "hast-util-select": "^4.0.2",
    "shiki": "^0.2.7"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
